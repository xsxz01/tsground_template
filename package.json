{
  "name": "{{project_name}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node ./esbuild.config.js",
    "build": "esbuild --bundle --sourcemap src/index.ts --outfile=dist/index.js --platform=node --target=node18,es2023"
  },
  "keywords": [],
  "repository": "https://github.com/xsxz01/tsground_template.git",
  "author": "{{author}}",
  "license": "{{license}}",
  "dependencies": {
    "@types/node": "^22.7.9",
    "gm-crypto": "^0.1.12",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "esbuild": "0.24.0"
  }
}
